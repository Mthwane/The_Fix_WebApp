{
  "_comment": "Reference config for the SQL Server variant of this app (e.g. Azure SQL Database). Copy the 'Database' and 'ConnectionStrings' blocks below into appsettings.json (or appsettings.Production.json) to switch the running app from Postgres to SQL Server - no code changes needed, Program.cs reads Database:Provider at startup.",

  "Database": {
    "Provider": "SqlServer"
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=tcp:yourserver.database.windows.net,1433;Initial Catalog=FashionFixDb;Persist Security Info=False;User ID=CHANGE_ME;Password=CHANGE_ME;MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
  },

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "InventorySettings": {
    "DefaultLowStockThreshold": 5
  }
}
