@model List<ProductVariant>
@{
    ViewData["Title"] = "Low Stock";
}
<div class="container-fluid py-4">
    <a asp-controller="Home" asp-action="Dashboard" class="btn btn-outline-secondary btn-sm mb-3">&larr; Dashboard</a>

    <h2>Low Stock</h2>
    <p class="text-muted">Every active size/colour currently at or below its style's restock threshold, most urgent first. Click Restock to jump straight to that product's Edit page.</p>

    @if (!Model.Any())
    {
        <div class="alert alert-success"><i class="bi bi-check-circle"></i> Nothing is low on stock right now.</div>
    }
    else
    {
        <table class="table table-hover align-middle">
            <thead><tr><th>Product</th><th>Size/Colour</th><th>SKU</th><th>Category</th><th>Current Stock</th><th>Threshold</th><th></th></tr></thead>
            <tbody>
                @foreach (var v in Model)
                {
                    var isOutOfStock = v.StockQuantity == 0;
                    <tr class="@(isOutOfStock ? "table-danger" : "table-warning")">
                        <td>@v.Product.Name</td>
                        <td>@v.Size / @v.Color</td>
                        <td>@v.SKU</td>
                        <td>@v.Product.Category</td>
                        <td>
                            <strong>@v.StockQuantity</strong>
                            @if (isOutOfStock)
                            {
                                <span class="badge bg-danger ms-1">Out of stock</span>
                            }
                        </td>
                        <td>@v.Product.LowStockThreshold</td>
                        <td class="text-end">
                            <a asp-controller="Products" asp-action="Edit" asp-route-id="@v.ProductId" class="btn btn-sm btn-danger">
                                <i class="bi bi-box-seam"></i> Restock
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
</div>
